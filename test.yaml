- name: install and run nginx
  hosts: web
  become: yes
  tasks:
  -task-1: install nginx
   ansible.builtin.package:
    name: nginx
    state: present
  -task-2: run nginx
   ansible.builtin.service:
    name: nginx
    state: started

   